scripted_gui = {
  intro_gui = {
    window_name = "lore_screen"
    context_type = player_context

    visible = {
		has_country_flag = unique_opening_screen
    }

    effects = {
      close_lore_screen_button_click = { clr_country_flag = unique_opening_screen }
    }

    triggers = {
    }

    dynamic_lists = {
	}

    properties = {
    }

    ai_enabled = {
		always = no
    }

    ai_test_interval = 290  # The higher, the long it takes the ai to do actions
	ai_test_variance = 0.5

    ai_check = {
    }

    ai_check_scope = {
    }

    ai_weights = {
    }
  }
  intro_button_left_gui = {
    window_name = "lore_open_button"
    context_type = player_context
    parent_window_name = lore_screen

    visible = {
		always = yes
    }

    effects = {
      lore_button01_click = { set_country_flag = unique_opening_screen }
    }

    triggers = {
    }

    dynamic_lists = {
	}

    properties = {
    }

    ai_enabled = {
		always = no
    }

    ai_test_interval = 290  # The higher, the long it takes the ai to do actions
	ai_test_variance = 0.5

    ai_check = {
    }

    ai_check_scope = {
    }

    ai_weights = {
    }
  }
  
}
